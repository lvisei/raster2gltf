import numpy as np

cells = np.array([[0, 1, 2], [0, 1, 3]])

print(cells)

print(cells+1)


# print(cells[:, ::-1])

# 定义一个奇异矩阵 A
# A = np.zeros((4, 4))
# A[0, -1] = 1
# A[-1, 0] = -1
# A = np.matrix(A)
# print(A)
